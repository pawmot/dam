jetty:
  host: "0.0.0.0"
  port: "80"

content-download-service:
  host: "dam-content-download"
  port: "80"

activemq:
  host: "activemq"
  user: "admin"
  pass: "admin"

spring:
  datasource:
    url: jdbc:mysql://db/dam?createDatabaseIfNotExist=true&useSSL=false&requireSSL=false
    username: root
    password: secr3t
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    database: mysql
    show-sql: true