jetty:
  host: "0.0.0.0"
  port: "8080"

spring:
  datasource:
    url: jdbc:mysql://localhost/dam?createDatabaseIfNotExist=true&useSSL=false&requireSSL=false
    username: root
    password: secr3t
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    database: mysql
    show-sql: true